This jQuery plugin gets the current location of the device and displays it inside a Google Map (when option is enabled). Intended to run in iOS, but also works in Chrome on a Windows desktop. 

Tested on Safari iOS 6.1.4 and Windows 7 Google Chrome Version 28.0.1500.95.